# 冻结训练执行方式修订

记录时间（UTC）：2026-07-25T15:33:30Z

冻结训练由串行执行改为单 GPU 上两个独立模型—种子任务并行执行。

本修订不改变模型定义、数据划分、公共目标支持、随机种子、batch size、AMP、优化器、学习率、最大轮数、早停规则、截止点或统计分析。

每个任务仍在独立 Python 进程中使用既定随机种子训练，并单独保存模型、缩放器、训练历史和审计文件。

Batch-4/5/6 外部 MATLAB payload 在此阶段仍未解析。
