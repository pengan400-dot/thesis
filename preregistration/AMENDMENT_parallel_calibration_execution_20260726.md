# 交叉拟合校准执行方式修订

交叉拟合校准由串行执行改为单张 GPU 上两个独立的 fold-seed 任务并行执行。

本修订只改变任务调度，不改变四折划分、随机种子、模型结构、batch size、学习率、优化器、最大轮数、早停规则、嵌套模型选择、校准评分、分位数计算或任何排除规则。

每个 fold-seed 任务在独立 Python 进程中运行，并分别保存模型、缩放器、训练历史和审计文件。已完成且通过哈希门禁的任务直接复用。

执行此修订时，Batch-4/5/6 MATLAB payload 仍未解析，外部实验结果仍未读取。
